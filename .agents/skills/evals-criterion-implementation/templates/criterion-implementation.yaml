component: "{component}"
criterion: "{criterion_id}"
version: 1

datasets:
  baseline:
    - "datasets/{criterion_id}/baseline.jsonl"
  # corner_cases:
  #   - "datasets/{criterion_id}/corner_cases.jsonl"
  # regression:
  #   - "datasets/{criterion_id}/regression.jsonl"

grader:
  family: "code" # code | llm_prompt | hybrid
  # Code-based:
  implementation: "graders.{criterion_id}:{GraderClass}"
  version: "1"
  # LLM prompt-based alternative:
  # runtime: "evals.shared.graders.prompt_grader:PromptGrader"
  # definition: "graders.{criterion_id}:definition"
  # version: "1"
  config: {}

required_artifact_fields: []

validation:
  fixtures: "grader-validation/{criterion_id}.jsonl"
  status: "passed" # passed | failed | not_run
  checks: []
  notes: ""
